package it.polimi.ingsw;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

//No implementation yet

class RoundTest {

    /*
    STILL TO CHECK

    Board board = new Board(3,true);
    SimpleBoardManager boardManager = new SimpleBoardManager(board);

    @Test
    void chooseAssistantCard() throws Exception {

        boardManager.login("Albus", Wizard.WIZARD1, TowersColor.WHITE);
        boardManager.login("Barb", Wizard.WIZARD2, TowersColor.BLACK);
        boardManager.login("Bozz", Wizard.WIZARD3, TowersColor.GRAY);

        Round round = null;

        round.chooseAssistantCard("Albus", AssistantCard.EIGHT);
        round.chooseAssistantCard("Barb", AssistantCard.THREE);
        round.chooseAssistantCard("Bozz", AssistantCard.FIVE);

        assertFalse(round.getChosenCards().isEmpty());

    }

    @Test
    void assignNextRound() throws Exception {

        boardManager.login("Albus", Wizard.WIZARD1, TowersColor.WHITE);
        boardManager.login("Barb", Wizard.WIZARD2, TowersColor.BLACK);
        boardManager.login("Bozz", Wizard.WIZARD3, TowersColor.GRAY);

        Round round = null;

        round.chooseAssistantCard("Albus", AssistantCard.EIGHT);
        round.chooseAssistantCard("Barb", AssistantCard.THREE);
        round.chooseAssistantCard("Bozz", AssistantCard.FIVE);

        assertFalse(round.getRoundOrder().isEmpty());
        assertFalse(round.getNextRoundOrder().isEmpty());

    }
*/
/*
    @Test
    void assignRoundOrder() {
    }

 */
/*
    //already tested in assignNextTurn()
    @Test
    void compareAssistantCards() throws Exception {

        boardManager.login("Albus", Wizard.WIZARD1, TowersColor.WHITE);
        boardManager.login("Barb", Wizard.WIZARD2, TowersColor.BLACK);
        boardManager.login("Bozz", Wizard.WIZARD3, TowersColor.GRAY);

        Round round = null;

        round.chooseAssistantCard("Albus", AssistantCard.EIGHT);
        round.chooseAssistantCard("Barb", AssistantCard.THREE);
        round.chooseAssistantCard("Bozz", AssistantCard.FIVE);

        assertFalse(round.getNextRoundOrder().isEmpty());

    }

 */
}
